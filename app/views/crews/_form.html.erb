<%= form_with(model: crew) do |form| %>
  <% if crew.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(crew.errors.count, "error") %> prohibited this crew from being saved:</h2>

      <ul>
        <% crew.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :sea, style: "display: block" %>
    <%= form.text_field :sea %>
  </div>

  <div>
    <%= form.label :island, style: "display: block" %>
    <%= form.text_field :island %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
