<!-- app/views/ships/show.html.erb -->
<div class="container">
  <div class="panel">
    <h1><%= @ship.name %></h1>

    <ul>
      <li><strong>Location:</strong> <%= @ship.location %></li>
      <li><strong>Crew:</strong>
        <% if @ship.crew %>
          <%= link_to @ship.crew.name, crew_path(@ship.crew) %>
        <% else %>
          —
        <% end %>
      </li>
    </ul>

    <p class="mt-3" style="display:flex; gap:12px; flex-wrap:wrap;">
      <%= link_to "Edit", edit_ship_path(@ship), class: "btn" %>
      <%= link_to "Back", ships_path, class: "btn secondary" %>
    </p>
  </div>
</div>
